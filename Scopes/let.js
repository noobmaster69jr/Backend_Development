/*If use let to declare a variable it always get the scope of enclosing block. if it is declared
outside it does not get the complete global scope.let does not allow redeclaration but var does*/

/*console.log(z) //error
let z = 15
*/

/*
let z = 10;
let z = 15
*/